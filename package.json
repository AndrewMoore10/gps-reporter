{
  "name": "gps-reporter",
  "version": "1.0.0",
  "description": "Node module that lists to gpsd for data and reports to a jsonapi-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "gps",
    "node",
    "jsonapi-server"
  ],
  "author": "Andrew Moore",
  "license": "ISC",
  "dependencies": {
    "node-gpsd": "^0.2.4",
    "redis": "^2.4.2",
    "serialport": "^2.0.5"
  }
}
